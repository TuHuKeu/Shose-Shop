import { slideChange } from './common/functions/slideChange.js';

function initSlideChangeClick({ whereSelector }) {
  slideChange(undefined, whereSelector);
}

export default initSlideChangeClick;
